{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/dispatch.py\"",
            "timeout": 60
          }
        ]
      },
      {
        "matcher": "ExitPlanMode",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'Plan mode exited. You may want to stress-test this plan before implementing it — run the plan-agent built-in Step 5b interview (or the review-plan team) to surface gaps, risks, and trade-offs.'"
          }
        ]
      }
    ]
  }
}
