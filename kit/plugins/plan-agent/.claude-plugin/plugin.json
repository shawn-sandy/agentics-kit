{
  "name": "plan-agent",
  "description": "Plan creation and review on demand — /plan-agent:implementation-plan skill runs the full Steps 1-8 workflow with a built-in structured interview; /plan-agent:review-plan skill runs a Workflow (7 core + 3 UI-conditional reviewers) that reviews, adversarially verifies high-severity findings, and updates implementation plans in place; /plan-agent:finalize-plan skill checks completion evidence and sets status; /plan-agent:build skill implements a plan end-to-end behind acceptance, end-to-end, and completion gates, and when the command names no plan it authors one first by chaining build-proposal and implementation-plan; /plan-agent:build-proposal skill turns a vague idea into a decision-complete proposal saved as a copy-pasteable prompt under docs/prompts/ via an 8-step research-then-decide loop with a Tier 0/1/2 gate, then hands off to implementation-plan; /plan-agent:build-feature skill turns a committed feature idea into a team feature doc under docs/features/ that splits into sized, dependency-ordered sub-feature plans, each with a paste-ready implementation-plan prompt saved under docs/prompts/; plans-library generates a filterable HTML gallery; /plan-agent:setup-sites skill scaffolds the GitHub Pages deploy pipeline into any repo so generated docs/ HTML publishes to a public URL; /plan-agent:prototype skill turns a completed HTML plan or a one-line idea into a runnable, framework-free static-HTML prototype under docs/prototypes/, auto-indexed into a Prototypes gallery; filename hook enforces verb-target kebab-case",
  "author": {
    "name": "Agentics Project"
  },
  "license": "MIT",
  "keywords": [
    "planning",
    "plan-agent",
    "planning",
    "on-demand",
    "conventions",
    "filename-validation",
    "workflow"
  ],
  "hooks": "./hooks.json",
  "homepage": "https://github.com/shawn-sandy/agentics-kit/tree/main/kit/plugins/plan-agent",
  "repository": "https://github.com/shawn-sandy/agentics-kit"
}
